# Pendientes, mejoras y correcciones — Epycus (Backend + Móvil)

> Generado el 2026-07-01 al cierre de una sesión larga de auditoría y fixes.
> Objetivo: que la siguiente sesión de IA pueda retomar sin tener que re-descubrir nada de esto.
> Metodología del proyecto: cambios mínimos y verificados; `dotnet build -c Release` y `dotnet test`
> antes de commitear; commit → push a `main` → despliega solo (GitHub Actions → VPS). Verificar
> siempre con `gh run watch <id>` y probando el endpoint en vivo. Repos:
> - Backend/Web: `C:\Users\marco\Pictures\EpycusApp` (GitHub Chester0802/EpycusApp, rama main)
> - Móvil Android: `C:\Users\marco\Pictures\Epycus` (GitHub Chester0802/epycus_movil, rama main)
> - Producción: https://app.epycus.es (VPS Debian, root@147.93.119.193:2222, ver credenciales.md)

---

## 0. Ya resuelto en esta sesión (no repetir)

Backend, todo desplegado y verificado en vivo contra producción:

1. **Data Protection keys no persistidas** → causaba el error real de "Cerrar Sesión" (`AntiforgeryValidationException: could not be decrypted`) y fallos intermitentes de Google Sign-In ("Correlation failed"). Se agregó `PersistKeysToFileSystem` en `/var/www/epycus-web/keys`.
2. **Sentry roto en cada arranque** (`AddSentry()` sin Dsn enlazado) → encubría otros errores bajo un 500 genérico en el primer log de Error tras cada reinicio. Corregido con `UseSentry(...)` explícito.
3. **`ApiBienestarController.ObtenerResumen` y `ServicioPomodoro.ObtenerTareasEnfoqueAsync`**: 5 y 2 tareas concurrentes respectivamente sobre el mismo `DbContext` (`Task.WhenAll`) → "A second operation was started...". Corregidos a secuencial.
4. **`ServicioAutenticacion.RenovarTokenInterno` y `ServicioIA.ChatAsync`**: transacción manual incompatible con `EnableRetryOnFailure` → fallaba el refresh token y el primer mensaje de cualquier chat. Envueltos en `CreateExecutionStrategy().ExecuteAsync(...)`.
5. **`ServicioBienestar.RegistrarEstadoAnimo`**: insertaba fila nueva cada vez en vez de upsert → duplicados por (UsuarioId, Fecha) rompían `Views/Bienestar/Index.cshtml` (`ToDictionary` con clave duplicada). Corregido a upsert + vista defensiva (`GroupBy`) + **índice único en BD** (con migración que limpia duplicados históricos antes de crearlo).
6. **FK sin validar antes de guardar** (mismo patrón, varios sitios): `CarreraId` (Registro/Perfil), `CategoriaId` (crear/editar Hábitos y Misiones), `TemaId` (cambiar tema) → `DbUpdateException` sin manejar (500). Todos con validación + mensaje de error claro (400).
7. **Guardas de body nulo/malformado** en `[FromBody]` que no las tenían: Login, Refresh, Chat IA, Actualizar/CambiarContrasena/CambiarPersonaje/CambiarTema de Perfil, Crear/Editar de Misiones y Hábitos, CrearFrase de Admin.
8. **`carreras()` exponía la entidad EF `Carrera` directamente** (con su colección de navegación `Usuarios`) → ahora usa `CarreraDto { Id, Nombre }`.
9. **5 eventos SignalR muertos** (Pomodoro ciclo/finalizado, Hábito completado, Misión completada, cambio de estado): el móvil ya tenía los handlers implementados pero el backend nunca los emitía. Se agregó `SendAsync` al grupo `usuario_{id}` en `ServicioPomodoro.RegistrarCiclo/FinalizarSesion`, `ServicioHabitos.CompletarHabito`, `ServicioMisiones.CompletarMision/CambiarEstado`.
10. **Gemini eliminado por completo** (ya no se usa esa API) — DeepSeek es ahora el único proveedor de IA. Se quitó: `IProveedorGemini`/`ProveedorGemini`, `GeminiHealthCheck`, HttpClient y rate limiter dedicados, config, y toda mención en docs/tests.
11. **CI/CD**: el health check del deploy exigía literalmente `"status":"Healthy"`, pero como Gemini estaba degradado, el estado global nunca podía serlo → **cualquier deploy se revertía solo** (rollback automático). Ahora acepta `"Healthy"` o `"Degraded"`.
12. **`.AsNoTracking()`** agregado en consultas de solo lectura (listados/dashboards/estadísticas) de: `ServicioBienestar`, `ServicioHabitos`, `ServicioMisiones`, `ServicioPomodoro`, `ServicioProgreso`, `ServicioDiarioAnimo`, `ServicioPerfil`, `ServicioAdmin`, `ServicioGamificacion`. Revisado método por método para no tocar ninguna consulta que luego se modifica y guarda en la misma llamada.
13. **JWT blacklist en memoria (`IJwtBlacklist` usaba `AddDistributedMemoryCache`)** → se perdía en cada reinicio del proceso (cada deploy), así que un token revocado por logout volvía a ser válido tras el siguiente despliegue. Se agregó tabla `TokensRevocados` (entidad `TokenRevocado`, migración `AgregarTokensRevocados`) con índice único por `Jti` e índice por `ExpiraEn`; `JwtBlacklist` ahora usa `ContextoAplicacion` directamente (ya era `Scoped`, igual que el `DbContext`) con limpieza oportunista de filas expiradas al insertar. Se quitó `AddDistributedMemoryCache()` de `ConfiguracionServicios.cs` (ya no se usaba para nada más). **Verificado en vivo:** login → GET `/api/v1/perfil` (200) → POST `/api/v1/auth/logout` (200) → mismo token en `/api/v1/perfil` → 401, confirmando revocación inmediata vía BD.
14. **Test flaky que bloqueaba el pipeline de CI/CD**: `ServicioPomodoroTests.EstadisticasAvanzadas_ConSesiones_CalculaCorrectamente` creaba una sesión "de hoy a las 10:00 UTC" y consultaba hasta "ahora" → si el CI corría entre las 00:00 y las 10:00 UTC, esa sesión quedaba en el futuro respecto al rango consultado y el test fallaba, bloqueando **cualquier** deploy en ese horario (se descubrió porque bloqueó el deploy del punto 13). Se reemplazaron las horas fijas (10/15) por desfases relativos a `DateTime.UtcNow` (siempre en el pasado). Confirmado que el fallo era preexistente (reproducido con `git stash` antes de los cambios de esta sesión) y no relacionado con ningún cambio de código de producto. 420/420 tests en verde tras el fix.
15. **IPv6 no escuchado en nginx.** Confirmado que el VPS sí tiene la IPv6 global asignada (`2a02:4780:2d:641d::1/48`) y que ufw ya permitía 80/443 en v6 — solo faltaba nginx. Se agregó `listen [::]:80;` y `listen [::]:443 ssl;`. **Hallazgo importante en el camino:** `/etc/nginx/sites-enabled/epycus-web` **no es symlink** de `sites-available/epycus-web` — son dos archivos independientes desincronizados desde el 25-jun (`sites-available` es el que edita Certbot). Solo se sincronizó lo autorizado: el header `proxy_set_header Connection $connection_upgrade;` (antes `"upgrade"` fijo en el archivo vivo, un bug real para SignalR/WebSockets bajo ciertos patrones de conexión) y el bloque de redirect adicional de Certbot (`if ($host = app.epycus.es) {...}`, insertado solo en el server de puerto 80; en un primer intento con `sed` se coló también en el server 443, lo que habría causado un loop de redirect infinito — corregido antes de recargar nginx, verificado que no hay loop). Verificado en vivo: `curl -6` a `/health` → 200, `curl` HTTP→HTTPS con 1 solo redirect (no loop), HTTPS directo → 200. Backups del archivo original en `/root/nginx-backups/` en el VPS.
16. **nginx: sintaxis `listen 443 ssl http2;` deprecada.** Separado en `listen 443 ssl;` + `http2 on;` en `sites-enabled/epycus-web` (el archivo realmente activo, ver punto 15). `nginx -t` sin warnings tras el cambio.
17. **Línea mal formada en el `.service` del VPS** (`Environment= Correo__NombreRemitente=Epycus App`, con un espacio suelto sin comillas después de `Environment=`). systemd tokenizaba por espacios y descartaba `App` (el warning `journalctl` original: "Invalid environment assignment, ignoring: App"), así que el remitente de correo quedaba truncado a "Epycus" en vez de "Epycus App". Corregido a `Environment="Correo__NombreRemitente=Epycus App"` (con comillas). `systemctl daemon-reload` + `restart`, verificado que el warning desaparece y `/health` sigue en 200.
18. **Paso "Apply EF Core migrations" del CI/CD.** Se instaló `dotnet-ef` global en el VPS (requiere `DOTNET_ROOT=/usr/share/dotnet` explícito o el apphost del tool falla con "Failed to resolve libhostfxr.so" aunque el runtime esté instalado — ya quedó instalado y confirmado con `dotnet-ef --version`). Pero el paso seguía sin poder funcionar de verdad: `dotnet ef database update` necesita el `.csproj` para resolver el `DbContext`, y el directorio publicado en el VPS solo tiene binarios ("No project was found", confirmado en el log de un deploy real). Implementar un EF bundle sería la forma correcta de que este paso funcione de verdad, pero es un cambio bastante más grande (tocar el job de Build & Publish, subir el bundle, resolver el connection string en runtime) — **se dejó fuera de esta sesión**. Se eliminó el paso redundante del workflow y se documentó que `Program.cs` (`Database.MigrateAsync()` al arrancar) es la única red de seguridad real, que es justamente lo que ha aplicado todas las migraciones hasta ahora.
19. **`.AsNoTracking()` en `ServicioAutenticacion`, `ServicioIA`, `ServicioCache`, `ConstructorContextoIA`** (quedaban pendientes de la sesión anterior). Revisado método por método; solo se tocó donde la entidad nunca se modifica/guarda en la misma llamada (lookups de Rol/Nivel por FK, fetch de usuario en refresh de token, `EnviarCorreoRecuperacion`, listado de carreras activas, historial de chat de IA, las 4 consultas cacheadas en memoria de `ServicioCache`, y todo `ConstructorContextoIA.ConstruirAsync` que es 100% lectura). No se tocó Login/CambiarContrasena/RestablecerContrasena/etc. porque esos sí modifican y guardan `Usuario` en la misma llamada.
20. **N+1 real encontrado y corregido en `ServicioGamificacion.VerificarYOtorgarLogros`**: por cada logro activo aún no obtenido por el usuario se repetía una query `CountAsync` (Hábitos/Misiones/SesionesPomodoro completados) dentro del `foreach` → hasta N queries redundantes por llamada, invocado en cada fin de ciclo Pomodoro. Los 3 contadores se calculan una sola vez antes del loop. Auditoría del resto del backend (16 servicios + 26 controllers) sin más hallazgos — confirmado independientemente dos veces con sub-agentes de exploración además de revisión manual propia.
21. **CI/CD: `Security Scan` (gitleaks) fallaba en falso** cuando un push traía 2+ commits juntos (`fetch-depth: 1` por defecto no tiene el historial para resolver el rango `before^..after` que usa gitleaks-action → "unknown revision", exit 1 sin haber escaneado nada, aparentando un secreto filtrado que no existía). Se agregó `fetch-depth: 0` al checkout de ese job. Confirmado que no era un secreto real: el mismo push, tras el fix, escaneó limpio.
22. **Pomodoro web se quedaba en tiempo negativo tras completar un ciclo** (reportado por el usuario: "Terminó la sesión 1, no salió el descanso corto, solo se detuvo, y al pulsar Continuar mostró -1:-1, luego -1:-2..."). Causa: `alCompletarTimer()` (`Views/Pomodoro/Index.cshtml`) podía abortar a medio camino (fetch fallido, modal de Bootstrap de "sugerir descanso largo" que no se muestra bien en móvil, etc.) dejando `tiempoRestante` congelado en negativo sin resetear el modo; al reanudar, el intervalo seguía restando desde ahí. Se agregó guarda de reentrancia (el tick normal del intervalo y la corrección de `visibilitychange` al volver de pantalla bloqueada podían llamar a la función dos veces) y un `try/catch/finally` que garantiza volver a un modo/tiempo válido ante cualquier fallo. `formatearTiempo()` también clampa a 0 como última red de seguridad. **Verificado:** build + 420/420 tests + sintaxis JS validada con `node --check`; **no se pudo confirmar visualmente en navegador** (sin acceso a Chrome en esta sesión) — pedirle al usuario que lo confirme reproduciendo el escenario.
23. **Botón "Cerrar Sesión" inalcanzable en móvil** (reportado por el usuario con capturas: el menú lateral en un viewport móvil no dejaba ver el botón, aunque "si existe pero está más abajo"). Causa: un fix anterior de esta misma issue ("RMOB-17: Sidebar overflow fix" en `wwwroot/css/site.css`) agregó `overflow-y: auto` a `.ep-sidebar`, pero sin `max-height` no había nada contra qué activar el scroll (`.ep-sidebar` solo tenía `min-height: 100vh`, sin tope) → en pantallas con poca altura el contenido (8 links de nav + toggle de tema + botón de cerrar sesión) crecía más alto que la pantalla, y al ser `position: fixed` sin límite de altura, el exceso (el botón de logout, al ser el último elemento) quedaba fuera del viewport e inalcanzable. Se agregó `max-height: 100vh` para que el `overflow-y: auto` ya existente funcione de verdad. **Mismo nivel de verificación que el punto 22** (build/tests OK, sin confirmación visual en navegador).
24. **Sesión web se perdía sin aviso al volver tras un rato, y de forma inconsistente entre páginas** (reportado por el usuario: cierra el navegador, usa el celular normal, reabre → "Inicio" se ve igual y recargar no cambia nada, pero al entrar a "Ajustes" manda a Login). Dos causas combinadas, ambas reales:
    - **`wwwroot/sw.js` cacheaba las navegaciones HTML con estrategia cache-first** (`event.request.mode === 'navigate'` caía en la rama "everything else: cache-first"). Una página servida desde el caché del Service Worker nunca vuelve a tocar el servidor, así que ni el estado de sesión ni el HTML/JS inline (ej. el fix del Pomodoro, que vive en un `<script>` dentro del `.cshtml`) se actualizaban jamás con un simple reload — esto también explica por qué los fixes 22/23 "no se veían por más que recargara". Cambiado a network-first para navegaciones (`CACHE_NAME` subido a `epycus-v3` para forzar la limpieza del caché viejo en cada cliente), con fallback a caché solo si de verdad no hay red.
    - **La cookie `jwt_token` (JWT de acceso, `Jwt:ExpiracionMinutos`=60 min) nunca se renovaba en el flujo web.** Existía una cookie `refresh_token` (de vida más larga, hasta 7 días si "Recordarme") pero nada la usaba — el mismo método `IServicioAutenticacion.RenovarToken` que ya usa el refresh del móvil estaba sin cablear al flujo web. Pasados los 60 minutos, cualquier página que realmente tocara el servidor (no servida de caché del punto anterior) redirigía a Login sin aviso. Se agregó un middleware en `Middleware/ConfiguracionMiddleware.cs`, justo antes de `UseAuthentication()`, que: si `jwt_token` falta o está por expirar (decodifica el claim `exp` sin validar firma, solo para decidir si conviene intentar renovar) y hay un `refresh_token` válido, llama a `RenovarToken`, setea las cookies nuevas, y redirige (302) a la misma URL — transparente para el usuario. Excluido de `/api`, `/admin` y `/Autenticacion` para no interferir con esos flujos.
    - **Verificado en vivo contra producción** (sin exponer tokens en el transcript, extraídos a variables de shell no persistidas): login → GET a una página protegida con **solo** la cookie `refresh_token` (sin `jwt_token`) → `302` con `Location` a la **misma página solicitada** (no a Login) + `Set-Cookie` con `jwt_token`/`refresh_token` nuevos → siguiendo el redirect completo (`curl -L`), `200` final. Confirma el flujo de renovación silenciosa de punta a punta.
    - Este mecanismo también corrige el problema hacia adelante para futuros usuarios: al ser server-side, aplica automáticamente a cualquier sesión sin necesitar más cambios por deploy.

Móvil (Android), **compilado, con commits locales SIN subir a GitHub** (`3a69d5f`, `61951c1`, `990f0bb` + el de esta sesión — el usuario solo autorizó push de backend hasta ahora, preguntar antes de subir):

13. **SignalR `RecibirAlerta`**: el backend envía UN objeto JSON, el cliente registraba el handler esperando 5 argumentos posicionales → deserialización fallaba en silencio, la alerta nunca llegaba. Corregido con un POJO (`AlertaNotificacionPayload`) y el mismo patrón para los 5 eventos nuevos del punto 9.
14. **Crash por post-detach (NPE/IllegalStateException)**: en `MisionesFragment`, `HabitosFragment`, `PerfilFragment` los callbacks de Retrofit tocaban `binding`/`requireView()` sin comprobar si la vista seguía viva. Se agregó guard `if (binding == null) return;` en los callbacks de mayor tráfico (carga de listas) y se protegieron los helpers `mostrarErrorRed()` / `actualizarChips()`. **No se cubrieron todos los callbacks secundarios** (ver sección de pendientes móvil, punto 2).
15. **Glide sin `.override(...)`**: 3 cargas del avatar en `PerfilFragment` decodificaban la imagen a resolución completa en un avatar pequeño (mismo bug ya arreglado antes en `InicioFragment`). Se agregó `.override(300, 600)` a las tres.
16. **`AuthInterceptor` — logout espurio por race condition**: si dos peticiones recibían 401 casi al mismo tiempo, la segunda no esperaba a que la primera terminara de refrescar el token, releía el token viejo, volvía a fallar y forzaba logout en medio de una sesión normal. Ahora usa `refreshLock.wait()`/`notifyAll()`.
17. **Pomodoro no notificaba en segundo plano** (prioridad alta de la sesión anterior): se implementó `AlarmManager.setExactAndAllowWhileIdle(...)` (con fallback a inexacta si no hay permiso, vía `canScheduleExactAlarms()`) programado/cancelado desde el único punto que ya persistía el estado del timer (`guardarEstadoTimer()`), un `PomodoroAlarmReceiver` nuevo (registrado en el manifest) y una clase `PomodoroNotificador` compartida entre el fragment (primer plano) y el receiver (segundo plano) para que la notificación sea idéntica y no se duplique. Esto también justifica el uso de `USE_EXACT_ALARM`/`SCHEDULE_EXACT_ALARM`, que antes estaban declarados sin usarse en ningún lado. Compilado (`compileDebugJavaWithJavac` en verde), **no probado en dispositivo/emulador real** (ninguno disponible en esta sesión).

---

## 1. Pendiente — Backend / Web

### Seguridad e infraestructura (prioridad alta)



1. **Deploy con usuario `root` + contraseña en vez de clave SSH.** La clave pública `github-actions-deploy-epycus` ya está en el VPS (`~/.ssh/authorized_keys` presumiblemente, verificar), pero el workflow (`.github/workflows/ci-cd.yml`) sigue usando `password: ${{ secrets.VPS_PASSWORD }}`. Falta: generar/localizar la clave privada correspondiente, agregarla como secret `VPS_SSH_KEY` en GitHub (Settings → Secrets → Actions), cambiar `appleboy/ssh-action` y `appleboy/scp-action` de `password:` a `key: ${{ secrets.VPS_SSH_KEY }}`, e idealmente migrar de `root` a un usuario `deploy` con sudo limitado solo a lo necesario (systemctl restart del servicio, chown del directorio de la app). **El usuario pidió explícitamente NO tocar esto en la sesión del 2026-07-02** — no asumir autorización previa, preguntar de nuevo.


### Producto / cumplimiento (prioridad baja, no bloquea nada técnico)

3. **Política de privacidad / Data Safety de Play Store para Edy IA + DeepSeek.** `web.md` ya tiene contenido completo de política de privacidad mencionando a DeepSeek como tercero (sección "Transferencias Internacionales" y "Servicios de Terceros"), pero confirmar que esté realmente publicado en `https://epycus.es/privacidad` y que el formulario "Data Safety" de Google Play Console esté completado acorde. También falta confirmar que exista un mecanismo real de borrado de cuenta (mencionado en Términos de Uso: "Ajustes > Eliminar cuenta") — verificar si esa funcionalidad existe en el backend/móvil o es solo texto legal por ahora.

---

## 2. Pendiente — Móvil (Android)

**Resuelto en esta sesión (commits locales, no subidos — ver `PENDIENTES.md` del móvil secciones 0.1 y 0.2):**
- Pomodoro ya notifica fin de ciclo/pausa aunque la app esté en segundo plano o el proceso muera, vía `AlarmManager.setExactAndAllowWhileIdle(...)` (`PomodoroAlarmReceiver` + `PomodoroNotificador` nuevos).
- Guards `binding == null` completados en los ~6 callbacks de sub-tarea que faltaban en `MisionesFragment`.
- `onNotificacionRecibida` ahora incluye `esCritica`; alertas críticas de bienestar muestran un `Snackbar` con color de error del tema en vez del mismo `Toast` discreto.
- `unirseAlGrupo` código muerto arreglado de verdad: se llama explícitamente tras conectar (mismo patrón que el frontend web), lo que también arregla el re-join tras reconectar.
- `bugs-pendientes.md` auditado: 4 de 6 bugs reales ya estaban resueltos por commits del 29/30-jun nunca marcados (avatar oscuro, selector de personajes, navegación en Pomodoro, ícono Edy); se aplicó además un fix preventivo al spinner de género (posible causa de contraste en modo oscuro).

Compilado (`compileDebugJavaWithJavac` en verde) en todos los casos, **nada probado en dispositivo/emulador real** por falta de uno disponible en esta sesión.

1. **Commits sin subir a GitHub**: `3a69d5f`, `61951c1`, `990f0bb`, más los 2 commits de esta sesión (Pomodoro en 2do plano, y guards/notif-críticas/unirseAlGrupo/bugs-pendientes). El usuario no ha autorizado push del repo móvil todavía — preguntar antes de hacerlo, no asumir.

2. **Revisar `AuditoriaUX.md` y `REVISION_APIS_Y_MOVIL.md`** (documento propio del repo móvil) por si quedan endpoints de la API que el móvil consume como `Object` sin tipar (`habitos/categorias`, `habitos/dashboard`, `ia/historial`, `ia/conversaciones`, `ia/sugerencias`, `ia/contexto-bienestar`, `ia/feedback` según el estado documentado el 24-jun) — bajo impacto funcional pero vale tipar para consistencia. No tocado en esta sesión.

---

## 3. Cómo verificar en producción (referencia rápida)

```
# Login de prueba (admin):
curl -s -X POST https://app.epycus.es/api/v1/auth/login -H "Content-Type: application/json" \
  -d '{"Correo":"admin@epycus.es","Contrasena":"Admin123@"}'

# Health check:
curl -s https://app.epycus.es/health

# Ver el último deploy:
gh run list --limit 1
gh run watch <id> --exit-status

# Logs del servicio (requiere SSH, ver credenciales.md):
journalctl -u epycus-web --no-pager --since '10 minutes ago'
```

No usar `cat`/`grep` directo sobre `/etc/systemd/system/epycus-web.service` para ver credenciales — el sistema de permisos lo bloquea (correctamente). Si se necesita verificar algo de la cadena de conexión, extraer el dato específico dentro de un mismo comando remoto sin imprimir el valor completo, o simplemente evitarlo y razonar con la migración/código en su lugar.

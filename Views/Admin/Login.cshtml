@model EpycusApp.ViewModels.Admin.AdminLoginViewModel

@{
    ViewData["Title"] = "Login Admin";
    Layout = "_LayoutAuth";
}

<div class="ep-card ep-card-neon p-4">
    <div class="text-center mb-4">
        <div class="d-inline-flex align-items-center justify-content-center mb-3" style="width: 64px; height: 64px; border-radius: 1rem; background: var(--ep-superficie-2); color: var(--ep-primario);">
            <i class="bi bi-shield-lock fs-2"></i>
        </div>
        <h2 class="fw-bold mb-1">Acceso Administrador</h2>
        <p class="text-muted small">Ãrea restringida para personal autorizado.</p>
    </div>

    <form asp-action="Login" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="mb-3">
            <label asp-for="CorreoElectronico" class="form-label small fw-semibold text-muted mb-2"></label>
            <input asp-for="CorreoElectronico" class="form-control ep-input" placeholder="admin@epycus.com" />
            <span asp-validation-for="CorreoElectronico" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Contrasena" class="form-label small fw-semibold text-muted mb-2"></label>
            <input asp-for="Contrasena" class="form-control ep-input" placeholder="â€¢â€¢â€¢â€¢â€¢â€¢â€¢â€¢" />
            <span asp-validation-for="Contrasena" class="text-danger small"></span>
        </div>

        <div class="form-check mb-3">
            <input asp-for="Recordarme" class="form-check-input" />
            <label asp-for="Recordarme" class="form-check-label small text-muted">Mantener sesiÃ³n</label>
        </div>

        <button type="submit" class="btn btn-ep w-100">Ingresar</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
